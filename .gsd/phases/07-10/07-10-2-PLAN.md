---
phase: 7
plan: 10-2
wave: 1
---

# Plan 07-10-2: Skeleton Loaders for Data-Heavy Views

## Objective
Create a reusable Skeleton UI primitive library and replace all spinner-only loading states in the three highest-visibility demo pages with contextual skeleton layouts.

## Context
- src/components/ui/Skeleton.tsx [NEW]
- src/pages/JobsPage.tsx
- src/features/seeker/components/Shortlist/ShortlistFeed.tsx
- src/pages/seeker/Dashboard.tsx

## Tasks

<task type="auto">
  <name>Create Skeleton.tsx primitive library</name>
  <files>src/components/ui/Skeleton.tsx [NEW]</files>
  <action>
    Create `src/components/ui/Skeleton.tsx` with:
    - `<Skeleton className>` — base `animate-pulse bg-gray-200` block
    - `<SkeletonJobCard>` — mirrors JobCard layout (title + company + meta + tags rows)
    - `<SkeletonShortlistCard>` — mirrors ShortlistCard (logo + title + meta + AI reasoning block + footer)
    - `<SkeletonDashboardCard>` — mirrors Profile Health card (header + 2-column content)
    All have `role="status"` and `aria-label` for accessibility.
  </action>
  <verify>File exists at `src/components/ui/Skeleton.tsx`; TypeScript compiles with 0 errors</verify>
  <done>All 4 skeleton components render without crashing</done>
</task>

<task type="auto">
  <name>Replace spinners with skeletons in JobsPage, ShortlistFeed, Dashboard</name>
  <files>
    src/pages/JobsPage.tsx,
    src/features/seeker/components/Shortlist/ShortlistFeed.tsx,
    src/pages/seeker/Dashboard.tsx
  </files>
  <action>
    JobsPage: Replace Loader2 spinner center div with 6× `<SkeletonJobCard>` in the same grid.
    ShortlistFeed:
      - Extract fetchShortlist as `useCallback` so "Try Again" button can call it (not window.reload)
      - Replace Loader2 spinner with 3× `<SkeletonShortlistCard>`
      - REMOVE the `DEBUG ERR: {shortlist.error}` div (line 68 in original) — this is user-facing
    Dashboard:
      - Replace full-screen Loader2 spinner with skeleton layout (header + 2-col grid using Skeleton components)
      - Dashboard keeps <Header> visible during load
      - Add Sentry.captureException to catch block
  </action>
  <verify>npx tsc --noEmit → 0 errors; npx vitest run tests/components/ → 27 passed</verify>
  <done>No spinner-only states in the three highest-visibility pages; DEBUG ERR permanently removed</done>
</task>

## Success Criteria
- [x] `src/components/ui/Skeleton.tsx` created with 4 components
- [x] `JobsPage` shows 6 skeleton cards while loading
- [x] `ShortlistFeed` shows 3 skeleton cards while loading
- [x] `ShortlistFeed` "Try Again" calls refetch (not page reload)
- [x] `DEBUG ERR: {shortlist.error}` permanently removed from ShortlistFeed
- [x] `Dashboard` shows skeleton layout (with Header visible) while loading
- [x] TypeScript `--noEmit` → 0 errors
- [x] 27 component tests pass

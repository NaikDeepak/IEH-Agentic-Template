---
phase: 7
plan: 10-3
wave: 2
---

# Plan 07-10-3: UX Polish Pass — Zero Console Errors & Demo Hardening

## Objective
Final demo readiness pass: upgrade PageLoader branding, replace all ad-hoc loading text strings with skeleton components, and add Sentry to all uncovered error paths — ensuring zero console errors on the investor demo happy path.

## Context
- src/App.tsx
- src/pages/seeker/Dashboard.tsx
- src/pages/JobsPage.tsx

## Tasks

<task type="auto">
  <name>Upgrade PageLoader and fix SeekerTrackerPage loading state</name>
  <files>src/App.tsx</files>
  <action>
    PageLoader: Replace plain spinner div with branded layout (IEH logo square + wordmark + spinner + "Loading..." mono text).
    SeekerTrackerPage loading state: Replace `"Loading Tracker..."` text div with 4 skeleton rows (border-2, animate-pulse rows).
    Add `import * as Sentry from '@sentry/react'` and wrap `console.error` calls in SeekerTrackerPage with `Sentry.captureException(error)`.
  </action>
  <verify>PageLoader renders IEH branding; SeekerTrackerPage shows skeleton rows during load; no bare console.error calls</verify>
  <done>PageLoader is branded; SeekerTrackerPage has skeleton rows; all errors captured by Sentry</done>
</task>

<task type="auto">
  <name>Add Sentry to remaining uncovered catch blocks</name>
  <files>src/pages/seeker/Dashboard.tsx, src/pages/JobsPage.tsx</files>
  <action>
    Dashboard.tsx catch block: add `Sentry.captureException(error)` before console.error.
    JobsPage.tsx already has console.error — add Sentry capture.
    Ensure imports are at top of file.
    These ensure that if any error occurs during the demo, it's captured silently in Sentry without a red console error trace.
  </action>
  <verify>npx tsc --noEmit → 0 errors; npx vitest run tests/components/ → all pass</verify>
  <done>All catch blocks in demo-critical pages have Sentry.captureException — zero uncaught errors on happy path</done>
</task>

## Success Criteria
- [x] `PageLoader` shows IEH branding (not plain spinner)
- [x] `SeekerTrackerPage` shows skeleton rows while loading
- [x] All `console.error` in demo-critical paths have matching `Sentry.captureException`
- [x] TypeScript `--noEmit` → 0 errors
- [x] 27 component tests pass

---
phase: 7
plan: 10-1
wave: 1
---

# Plan 07-10-1: Toast & Global Feedback System

## Objective
Install `sonner` and wire a global toast/notification system so every user action has clear, dismissible feedback. Adds success/error toasts to the two highest-investor-impact actions: apply and resume analysis.

## Context
- src/App.tsx
- src/components/ApplyModal.tsx
- src/features/seeker/components/ResumeAnalyzer/ResumeAnalyzer.tsx

## Tasks

<task type="auto">
  <name>Install sonner and wire Toaster into App.tsx</name>
  <files>src/App.tsx</files>
  <action>
    Run `npm install sonner`. Add `import { Toaster } from 'sonner'` to App.tsx.
    Render `<Toaster position="bottom-right" richColors toastOptions={{ classNames: { toast: 'font-mono text-xs uppercase tracking-widest border-2 border-black shadow-[4px_4px_0px_0px_rgba(0,0,0,1)]' } }}>`
    inside `<Router>` before `<Suspense>`. This provides access to toast() in any component in the tree.
    Also upgrade PageLoader to show IEH branding (logo square + wordmark + spinner + text).
  </action>
  <verify>Toaster element in DOM; toast() callable from any component; PageLoader shows "IEH" branding</verify>
  <done>sonner installed; Toaster renders in page; PageLoader is branded</done>
</task>

<task type="auto">
  <name>Add toasts to ApplyModal and ResumeAnalyzer</name>
  <files>src/components/ApplyModal.tsx, src/features/seeker/components/ResumeAnalyzer/ResumeAnalyzer.tsx</files>
  <action>
    ApplyModal: import `toast` from sonner, add `toast.success('Application submitted! ...')` after setStep('success'),
    and `toast.error(msg)` in the catch block alongside existing inline error.
    Also add `import * as Sentry from '@sentry/react'` and `Sentry.captureException(err)` in catch blocks.
    ResumeAnalyzer: same pattern — toast.success on analysis complete, toast.error + Sentry on failure.
  </action>
  <verify>npx tsc --noEmit → 0 errors; npx vitest run tests/components/ → 27 passed</verify>
  <done>Toasts fire on apply success/error and resume analyze success/error</done>
</task>

## Success Criteria
- [x] `sonner` installed and `<Toaster>` renders globally
- [x] `toast.success(...)` fires when application is submitted
- [x] `toast.error(...)` fires when application fails
- [x] `toast.success(...)` fires when resume analysis completes
- [x] `Sentry.captureException` added to all new/modified catch blocks
- [x] TypeScript `--noEmit` → 0 errors
- [x] 27 component tests pass

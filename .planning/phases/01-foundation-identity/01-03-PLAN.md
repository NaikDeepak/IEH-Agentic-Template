---
phase: 01-foundation-identity
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/layouts/AdminLayout.tsx, src/pages/admin/AdminDashboard.tsx, src/components/admin/AdminSidebar.tsx, src/App.tsx]
autonomous: true
---

<objective>
Create visual scaffolding for the Super Admin dashboard.
Purpose: Requirement AUTH-03 (Super Admin dashboard).
Output: Admin layout and placeholder dashboard page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create Admin Layout</name>
  <files>src/layouts/AdminLayout.tsx, src/components/admin/AdminSidebar.tsx</files>
  <action>
    1. Create `AdminSidebar` with links: Dashboard, Users, Jobs, Settings.
    2. Create `AdminLayout` that wraps content with the sidebar.
    3. Use Tailwind for basic styling (distinct from main app if possible, or consistent).
  </action>
  <verify>npm run build succeeds</verify>
  <done>Layout components created</done>
</task>

<task type="auto">
  <name>Task 2: Create Admin Dashboard Page</name>
  <files>src/pages/admin/AdminDashboard.tsx</files>
  <action>
    Create a dashboard view with placeholder stats cards:
    - Total Users
    - Active Jobs
    - Total Applications
  </action>
  <verify>npm run build succeeds</verify>
  <done>Dashboard page created</done>
</task>

<task type="auto">
  <name>Task 3: Register Admin Route</name>
  <files>src/App.tsx</files>
  <action>
    Add `/admin` route to App.tsx using AdminLayout.
    (Note: Protection will be added in Plan 02, just wire it up here).
  </action>
  <verify>npm run build succeeds</verify>
  <done>Admin route accessible</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds
- [ ] Admin pages render correctly
</verification>

<success_criteria>
- Admin layout exists with navigation
- Admin dashboard renders
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-identity/01-03-SUMMARY.md`
</output>

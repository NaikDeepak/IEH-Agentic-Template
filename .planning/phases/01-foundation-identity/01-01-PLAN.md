---
phase: 01-foundation-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/firebase.ts, src/context/AuthContext.ts, src/context/AuthProvider.tsx, src/components/Login.tsx, src/pages/Register.tsx, src/App.tsx]
autonomous: true
---

<objective>
Add Email/Password authentication support to the existing Google-only system.
Purpose: Requirement AUTH-01 (Users can sign up/login as Job Seeker or Employer via Email/Password).
Output: Updated AuthProvider with email methods, new Register page, updated Login component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/firebase.ts
@src/context/AuthProvider.tsx
@src/components/Login.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Update Firebase exports</name>
  <files>src/lib/firebase.ts</files>
  <action>Export `signInWithEmailAndPassword`, `createUserWithEmailAndPassword`, and `signOut` from firebase/auth. Ensure `auth` instance is correctly exported.</action>
  <verify>Check exports exist in file</verify>
  <done>Firebase auth methods available for import</done>
</task>

<task type="auto">
  <name>Task 2: Update Auth Context</name>
  <files>src/context/AuthContext.ts, src/context/AuthProvider.tsx</files>
  <action>
    1. Update AuthContextType to include `loginWithEmail`, `signupWithEmail` methods.
    2. Implement these methods in AuthProvider using Firebase SDK.
    3. Ensure `loading` state is handled correctly during async auth calls.
    4. Keep existing Google auth logic intact.
  </action>
  <verify>TypeScript compiles without errors</verify>
  <done>AuthContext exposes email/password methods</done>
</task>

<task type="auto">
  <name>Task 3: Create Register Page</name>
  <files>src/pages/Register.tsx, src/App.tsx</files>
  <action>
    1. Create a registration form with Name, Email, Password, Confirm Password.
    2. On submit, call `signupWithEmail`.
    3. On success, redirect to RoleSelection (or Dashboard if role exists).
    4. Add link to Login page.
    5. Add route `/register` to App.tsx.
  </action>
  <verify>npm run build succeeds</verify>
  <done>Register page exists and is routed</done>
</task>

<task type="auto">
  <name>Task 4: Update Login Component</name>
  <files>src/components/Login.tsx</files>
  <action>
    1. Add Email/Password form to the existing Login component (likely in the "card" variant or a new modal/page view).
    2. Keep "Sign in with Google" as an option.
    3. Call `loginWithEmail` on form submit.
    4. Add link to Register page.
  </action>
  <verify>npm run build succeeds</verify>
  <done>Login component supports email/password</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds
- [ ] No TypeScript errors
</verification>

<success_criteria>
- Users can sign up with email/password
- Users can login with email/password
- Existing Google login still works
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-identity/01-01-SUMMARY.md`
</output>

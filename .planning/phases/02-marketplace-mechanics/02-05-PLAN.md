---
phase: 02-marketplace-mechanics
plan: 05
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/components/StatusBadge.tsx, src/components/JobCard.tsx]
autonomous: true
must_haves:
  truths:
    - "User can see if a job is Active or Passive"
    - "Passive items are visually distinct"
  artifacts:
    - path: "src/components/StatusBadge.tsx"
      provides: "Reusable UI component for status"
---

<objective>
Implement UI indicators for the Active System.

Purpose: Visual feedback for the underlying mechanics.
Output: UI components.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepaknaik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-marketplace-mechanics/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StatusBadge Component</name>
  <files>src/components/StatusBadge.tsx</files>
  <action>
    Create a reusable component `StatusBadge` that accepts `status` ('active' | 'passive') and optional `expiresAt`.

    Visuals:
    - Active: Green dot/badge.
    - Passive: Gray/Red dot/badge.
    - Tooltip (if expiresAt provided): "Expires in X days".

    Use Tailwind and Lucide icons.
  </action>
  <verify>
    File exists.
  </verify>
  <done>
    Component created.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create/Update JobCard</name>
  <files>src/components/JobCard.tsx</files>
  <action>
    Create or update `JobCard` to display the `StatusBadge`.
    Ensure it gracefully handles the new `status` field from the Job type.
  </action>
  <verify>
    grep "StatusBadge" src/components/JobCard.tsx
  </verify>
  <done>
    Job listings show status.
  </done>
</task>

</tasks>

<verification>
Manual visual check of job lists and badge appearance.
</verification>

<success_criteria>
- [ ] StatusBadge component exists
- [ ] JobCard shows status
</success_criteria>

<output>
After completion, create `.planning/phases/02-marketplace-mechanics/02-05-SUMMARY.md`
</output>

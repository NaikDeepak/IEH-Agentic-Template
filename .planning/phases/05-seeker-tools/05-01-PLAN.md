---
phase: 05-seeker-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/features/seeker/types.ts, src/features/seeker/components/ResumeAnalyzer/ResumeInput.tsx]
autonomous: true

must_haves:
  truths:
    - "Seeker feature directory structure exists"
    - "User can toggle between Upload and Paste modes"
    - "User sees instructions for LinkedIn PDF Export"
  artifacts:
    - path: "src/features/seeker/types.ts"
      provides: "Seeker domain types"
    - path: "src/features/seeker/components/ResumeAnalyzer/ResumeInput.tsx"
      provides: "Resume entry UI"
  key_links:
    - from: "src/features/seeker/components/ResumeAnalyzer/ResumeInput.tsx"
      to: "parent component"
      via: "onSubmit callback"
---

<objective>
Initialize the seeker feature infrastructure and create the multi-source resume input UI.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepaknaik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seeker-tools/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Seeker Feature Structure and Schema</name>
  <files>src/features/seeker/types.ts</files>
  <action>
    Create the directory structure for the seeker feature.
    Define Firestore schema expectations:
    - `users/{uid}/resumes`: Parsed resume data and ATS scores.
    - `users/{uid}/applications`: Reference to job applications.
    - `users/{uid}/shortlist`: Daily job recommendations.
    - `users/{uid}/skillGaps`: Identified gaps and resources.

    Define types in `src/features/seeker/types.ts` for:
    - SeekerProfile (linked to Auth user)
    - ResumeAnalysisResult (score, keywords, suggestions, metadata)
    - Application (extending existing application types)
    - SkillGap (role, missing skills, resources)
  </action>
  <verify>ls src/features/seeker/types.ts && npx tsc src/features/seeker/types.ts --noEmit</verify>
  <done>src/features/seeker/ structure exists and types.ts is defined with explicit schema support.</done>
</task>

<task type="auto">
  <name>Task 2: Create Resume Input UI Component</name>
  <files>src/features/seeker/components/ResumeAnalyzer/ResumeInput.tsx</files>
  <action>
    Implement a tabbed UI component that allows users to:
    1. Upload a file (PDF/DOCX)
    2. Paste raw text
    3. LinkedIn PDF Export: Provide clear instructions/UI for users to upload their LinkedIn exported PDF (Research indicates API is restricted).
    Use Tailwind for styling.
  </action>
  <verify>ls src/features/seeker/components/ResumeAnalyzer/ResumeInput.tsx && grep -E "Upload|Paste|LinkedIn" src/features/seeker/components/ResumeAnalyzer/ResumeInput.tsx</verify>
  <done>User can input resume data via file, text, or guided LinkedIn PDF upload.</done>
</task>

</tasks>

<verification>
Check directory structure and component file existence.
</verification>

<success_criteria>
Seeker scaffolding is ready for service implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/05-seeker-tools/05-01-SUMMARY.md`
</output>

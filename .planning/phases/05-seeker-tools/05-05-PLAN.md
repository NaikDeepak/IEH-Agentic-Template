---
phase: 05-seeker-tools
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [functions/src/marketProxy.ts, functions/src/index.ts, src/features/seeker/services/marketService.ts, src/features/seeker/components/Market/MarketTrends.tsx]
autonomous: true
user_setup:
  - service: Adzuna
    why: "Market salary data"
    env_vars:
      - name: ADZUNA_APP_ID
        source: "Adzuna Developer Dashboard"
      - name: ADZUNA_APP_KEY
        source: "Adzuna Developer Dashboard"

must_haves:
  truths:
    - "API keys are not exposed to the frontend"
    - "Seeker sees salary ranges for their target role"
  artifacts:
    - path: "functions/src/marketProxy.ts"
      provides: "Secure API proxy"
    - path: "src/features/seeker/services/marketService.ts"
      provides: "Market data fetching"
    - path: "src/features/seeker/components/Market/MarketTrends.tsx"
      provides: "Market data UI"
  key_links:
    - from: "marketService.ts"
      to: "Firebase Cloud Function"
      via: "httpsCallable"
---

<objective>
Setup a secure proxy for the Adzuna API to provide market salary insights and trends.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepaknaik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-seeker-tools/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Market Proxy Cloud Function</name>
  <files>functions/src/marketProxy.ts, functions/src/index.ts</files>
  <action>
    Implement an HTTPS callable Firebase Function that proxies requests to Adzuna's Market Insights API.
    Use environment variables for API keys.
    Export the function in `functions/src/index.ts`.
  </action>
  <verify>grep "onCall" functions/src/marketProxy.ts && grep "marketProxy" functions/src/index.ts</verify>
  <done>Secure access to Adzuna market data via Cloud Function.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Market Service and UI</name>
  <files>src/features/seeker/services/marketService.ts, src/features/seeker/components/Market/MarketTrends.tsx</files>
  <action>
    Implement frontend service to call the proxy.
    Create a UI component `MarketTrends.tsx` to display salary ranges and trend indicators for a given role.
  </action>
  <verify>ls src/features/seeker/components/Market/MarketTrends.tsx && grep "salary" src/features/seeker/components/Market/MarketTrends.tsx</verify>
  <done>Market insights are available and rendered for seekers.</done>
</task>

</tasks>

<verification>
Check function exports and UI component existence.
</verification>

<success_criteria>
Seekers have access to real-time salary and market data securely.
</success_criteria>

<output>
After completion, create `.planning/phases/05-seeker-tools/05-05-SUMMARY.md`
</output>

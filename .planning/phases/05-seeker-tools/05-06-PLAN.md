---
phase: 05-seeker-tools
plan: 06
type: execute
wave: 4
depends_on: [05-04]
files_modified: [src/features/seeker/services/shortlistService.ts, src/features/seeker/components/Shortlist/ShortlistFeed.tsx]
autonomous: true

must_haves:
  truths:
    - "User sees exactly 5 recommended jobs per day"
    - "Recommendations match the skills in the user profile/resume"
    - "New users see a 'Complete your profile' CTA instead of empty results"
  artifacts:
    - path: "src/features/seeker/services/shortlistService.ts"
      provides: "Curation logic"
    - path: "src/features/seeker/components/Shortlist/ShortlistFeed.tsx"
      provides: "Daily digest UI with cold start handling"
  key_links:
    - from: "shortlistService.ts"
      to: "Vector Search / Job Collection"
      via: "Semantic query"
---

<objective>
Implement the "Smart Job Shortlist" feature providing a daily digest of top matches, handling cold starts gracefully.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepaknaik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-seeker-tools/05-CONTEXT.md
@src/features/jobs/services/jobService.ts
@.planning/phases/05-seeker-tools/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Shortlist Matching Logic</name>
  <files>src/features/seeker/services/shortlistService.ts</files>
  <action>
    Implement logic to find the "Top 5" jobs for a user based on their persisted resume/profile skills (from Plan 04).
    Use existing vector search capabilities.
    Ensure results are "static" for 24h.
    Handle "Cold Start": If no resume/skills exist, return a specific empty/initial state signal.
  </action>
  <verify>grep "getDailyShortlist" src/features/seeker/services/shortlistService.ts && npx tsc src/features/seeker/services/shortlistService.ts --noEmit</verify>
  <done>System can generate a personalized job shortlist or signal a cold start state.</done>
</task>

<task type="auto">
  <name>Task 2: Create Shortlist UI Feed with Cold Start UI</name>
  <files>src/features/seeker/components/Shortlist/ShortlistFeed.tsx</files>
  <action>
    Implement the "Daily Digest" view.
    Use a card layout to present the 5 recommended jobs.
    Include a "Why this matches" snippet.
    CRITICAL: Handle Cold Start gracefully. If the service returns a cold start signal (new user, no resume), show a "Complete your profile" CTA with a link to the Resume Analyzer instead of an empty list.
  </action>
  <verify>ls src/features/seeker/components/Shortlist/ShortlistFeed.tsx && grep "Complete your profile" src/features/seeker/components/Shortlist/ShortlistFeed.tsx</verify>
  <done>User has a high-quality job feed or a clear CTA to provide data if missing.</done>
</task>

</tasks>

<verification>
Check for existence of service and UI components.
</verification>

<success_criteria>
Relevant, curated job recommendations delivered daily.
</success_criteria>

<output>
After completion, create `.planning/phases/05-seeker-tools/05-06-SUMMARY.md`
</output>

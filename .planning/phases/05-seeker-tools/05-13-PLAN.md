---
phase: 05-seeker-tools
plan: 13
type: execute
wave: 1
depends_on: [12]
files_modified: [.env.example, functions/.env.example]
autonomous: false
gap_closure: true
user_setup:
  - service: google-gemini
    why: "Resume analysis and skill gap scoring"
    env_vars:
      - name: VITE_GEMINI_API_KEY
        source: "Google AI Studio -> Get API Key"
  - service: adzuna
    why: "Real-time market salary data"
    env_vars:
      - name: ADZUNA_APP_ID
        source: "Adzuna Dashboard -> API Keys"
      - name: ADZUNA_APP_KEY
        source: "Adzuna Dashboard -> API Keys"

must_haves:
  truths:
    - "Environment templates contain all required keys for Phase 05"
    - "User is prompted to provide credentials for Gemini and Adzuna"
  artifacts:
    - path: ".env.example"
      contains: "VITE_GEMINI_API_KEY"
    - path: "functions/.env.example"
      contains: "ADZUNA_APP_ID"
---

<objective>
Resolve Phase 05 UAT blockers by standardizing environment variables and providing a guided setup for required API credentials.

Purpose: Fix "API key missing" and "Connection failure" errors in Seeker tools.
Output: Updated .env.example files and a blocking setup checkpoint.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-seeker-tools/05-UAT.md
@.planning/phases/05-seeker-tools/05-04-SUMMARY.md
@.planning/phases/05-seeker-tools/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Update Environment Templates</name>
  <files>.env.example, functions/.env.example</files>
  <action>
    1. Append VITE_GEMINI_API_KEY to the root .env.example.
    2. Create or update functions/.env.example to include ADZUNA_APP_ID and ADZUNA_APP_KEY.
    3. Ensure placeholders are clear for the user.
  </action>
  <verify>grep for keys in both .env.example files.</verify>
  <done>Templates contain placeholders for all external service credentials used in Phase 05.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Provision API Credentials</action>
  <instructions>
    The Seeker tools require external API keys to function:
    1. Get a Gemini API Key from: https://aistudio.google.com/
    2. Add it to your root .env as: VITE_GEMINI_API_KEY=...
    3. Get Adzuna API credentials from: https://developer.adzuna.com/
    4. Add them to functions/.env as:
       ADZUNA_APP_ID=...
       ADZUNA_APP_KEY=...
    5. Restart the dev server and re-deploy functions (firebase deploy --only functions).
  </instructions>
  <resume-signal>I have added the keys and restarted the services.</resume-signal>
</task>

</tasks>

<verification>
Check if Resume Analysis and Market Trends now load without "API key missing" errors.
</verification>

<success_criteria>
- Seeker Dashboard widgets load without connection errors.
- Resume upload triggers analysis instead of immediate failure.
</success_criteria>

<output>
After completion, create .planning/phases/05-seeker-tools/05-13-SUMMARY.md
</output>
